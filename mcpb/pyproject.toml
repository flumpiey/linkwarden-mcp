[project]
name = "linkwarden-mcp-desktop"
version = "0.1.5"
requires-python = ">=3.10"
# Keep empty: Claude Desktop `server.type=uv` syncs this file at install time.
# Runtime pulls the PyPI package via `uv tool run` in manifest mcp_config.
dependencies = []
